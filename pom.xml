<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>


    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <SlayTheSpire.version>12-22-2020</SlayTheSpire.version>
        <ModTheSpire.version>3.30.3</ModTheSpire.version>
        <modID>bobloypatches</modID>

        <!--CHANGE THIS TO YOUR STEAM INSTALLATION-->
        <steam.path>C:/Program Files (x86)/Steam/steamapps/</steam.path>
        <sts.path>/common/SlayTheSpire/desktop-1.0.jar</sts.path>
        <workshop.path>/workshop/content/646570/</workshop.path>

        <author>Bobloy</author>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    </properties>

    <groupId>${modID}</groupId>
    <artifactId>${modID}</artifactId>
    <version>0.0.5</version>
    <name>BobloyPatches</name>
    <description>TiS Multiplayer Compatability patches.</description>
    <!-- Change the above for mod information. -->

    <dependencies>
        <dependency>
            <groupId>com.megacrit.cardcrawl</groupId>
            <artifactId>slaythespire</artifactId>
            <version>${SlayTheSpire.version}</version>
            <scope>system</scope>
            <systemPath>${steam.path}${sts.path}</systemPath>
        </dependency>
        <dependency>
            <groupId>com.evacipated.cardcrawl</groupId>
            <artifactId>modthespire</artifactId>
            <version>${ModTheSpire.version}</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/1605060445/ModTheSpire.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>basemod</groupId>
            <artifactId>basemod</artifactId>
            <!--Don't worry about these version numbers, they're only used for display
                in the left sidebar under External Libraries and don't need to be accurate.-->
            <version>5.56.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/1605833019/BaseMod.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.evacipated.cardcrawl.mod</groupId>
            <artifactId>StSLib</artifactId>
            <version>2.4.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/1609158507/StSLib.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>panstrinkets</groupId>
            <artifactId>PansTrinkets</artifactId>
            <version>1.0.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/2805998622/PansTrinkets.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>blockreminder</groupId>
            <artifactId>block-reminder</artifactId>
            <version>1.2.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/1974165911/block-reminder.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>tiscardpack</groupId>
            <artifactId>tisCardPack</artifactId>
            <version>1.0.5</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/3148331689/TogetherInSpireBoosterPack.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>spiretogether</groupId>
            <artifactId>SpireTogether</artifactId>
            <version>2.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/2384072973/TogetherInSpire.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>CardAugments</groupId>
            <artifactId>CardAugments</artifactId>
            <version>1.0.5</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/2970981743/CardAugments.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>conspire</groupId>
            <artifactId>Conspire</artifactId>
            <version>1.7.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/1612329757/Conspire.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>aspiration</groupId>
            <artifactId>Aspiration</artifactId>
            <version>1.7.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/1611701846/Aspiration.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>evolution</groupId>
            <artifactId>Evolution</artifactId>
            <version>1.7.0</version>
            <scope>system</scope>
            <systemPath>${steam.path}${workshop.path}/2339471027/Evolution.jar</systemPath>
        </dependency>
    </dependencies>


    <!-- This is how your code is packaged into the jar file-->
    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>com.github.sylvainlaurent.maven</groupId>
                <artifactId>yaml-json-validator-maven-plugin</artifactId>
                <version>1.0.4</version>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>validate</goal>
                        </goals>
                        <configuration>
                            <validationSets>
                                <validationSet>
                                    <includes>
                                        <include>src/main/resources/**/*.json</include>
                                    </includes>
                                </validationSet>
                            </validationSets>
                            <allowEmptyFiles>true</allowEmptyFiles>
                            <skip>false</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <excludes>
                        <exclude>**/*.psd</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                    <delimiters>
                        <delimiter>${*}</delimiter>
                    </delimiters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>description</id>
                        <phase>prepare-package</phase>
                        <configuration>
                            <target>
                                <property name="tempDescription" value="${project.description}"/>
                                <loadresource property="filteredDescription">
                                    <propertyresource name="tempDescription"/>
                                    <filterchain>
                                        <tokenfilter>
                                            <filetokenizer/>
                                            <replaceregex pattern="(\s*\n\s*)" replace="\\\\n" flags="g"/>
                                            <replaceregex pattern='((\\\\)*)(\\?")' replace='\1\\\\"' flags="g"/>
                                        </tokenfilter>
                                    </filterchain>
                                </loadresource>

                                <!--suppress UnresolvedMavenProperty -->
                                <replace file="target/classes/ModTheSpire.json" value="${filteredDescription}">
                                    <replacetoken>!(project.description)</replacetoken>
                                </replace>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>copy</id>
                        <phase>package</phase>
                        <configuration>
                            <target>
                                <!--This puts a copy of your mod's jar file into the SlayTheSpire mods folder so it will be loaded.-->
                                <!--On mac, it will be copied to another location. See further below.-->
                                <copy file="target/${project.artifactId}.jar" tofile="${steam.path}/common/SlayTheSpire/mods/${project.artifactId}.jar"/>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes> <!-- These files won't not be filtered. -->
                    <exclude>ModTheSpire.json</exclude>
                    <exclude>**/*.json</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes> <!-- These files will be filtered. -->
                    <include>**/*.json</include>
                </includes>
            </resource>
            <!-- Filtering replaces certain text like ${modID} with the corresponding value from this file. -->
        </resources>
    </build>

    <profiles>
        <profile>
            <id>default</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
<!--                <steam.path>${steam.windows}</steam.path>-->
<!--                <sts.path>/common/SlayTheSpire/desktop-1.0.jar</sts.path>-->
            </properties>
        </profile>
    </profiles>
</project>